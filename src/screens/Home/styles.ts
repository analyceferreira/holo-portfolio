import styled, { css } from 'styled-components'

export const Container = styled.div`
    width: 100%;
    height:100%
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    z-index: 0;
`;